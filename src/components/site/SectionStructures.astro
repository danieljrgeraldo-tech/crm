---
import { site } from "../../data/site-content.js";
import SectionLabel from "./SectionLabel.astro";
import MotionText from "./MotionText.astro";

const content = site.structures;
---

<section
  id="estruturas"
  class="relative bg-paper py-16 lg:py-20"
  aria-labelledby="structures-title"
>
  <div class="pointer-events-none absolute inset-0 opacity-30" aria-hidden="true">
    <div class="technical-line top-0 left-[6%] h-full w-px"></div>
    <div class="technical-line top-0 right-[6%] h-full w-px hidden lg:block"></div>
  </div>

  <div class="content-container relative z-10">
    <SectionLabel text={content.eyebrow} />

    <h2
      id="structures-title"
      class="mt-5 max-w-3xl font-display text-4xl font-light leading-[1.05] tracking-tight text-ink sm:text-5xl lg:text-6xl"
    >
      <MotionText as="span" class="block" type="reveal-line">
        {content.title}
      </MotionText>
    </h2>

    <MotionText as="p" class="mt-5 max-w-2xl text-base leading-relaxed text-graphite lg:mt-6 lg:text-lg" type="reveal">
      {content.intro}
    </MotionText>

    <div class="structures-list mt-10 border-t border-line lg:mt-12">
      {content.items.map((item, i) => (
        <div
          class="structure-item group grid grid-cols-1 gap-2 border-b border-line py-5 transition-colors hover:bg-paper-light sm:grid-cols-[auto_1fr] sm:gap-6 sm:py-5 lg:gap-10"
          data-motion="reveal"
        >
          <span class="font-sans text-xs font-medium uppercase tracking-widest text-muted">
            {String(i + 1).padStart(2, "0")}
          </span>
          <div>
            <h3 class="font-display text-xl font-light text-ink sm:text-2xl lg:text-2xl">
              {item.title}
            </h3>
            <p class="mt-1 max-w-2xl text-sm leading-relaxed text-muted lg:text-base">
              {item.text}
            </p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
