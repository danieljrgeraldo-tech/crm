---
import { site } from "../../data/site-content.js";
import SectionLabel from "./SectionLabel.astro";
import MotionText from "./MotionText.astro";

const content = site.method;
---

<section
  id="metodo"
  class="relative bg-paper-light py-16 lg:py-20"
  aria-labelledby="method-title"
>
  <div class="pointer-events-none absolute inset-0 opacity-30" aria-hidden="true">
    <div class="technical-line top-0 left-[6%] h-full w-px"></div>
    <div class="technical-line top-0 right-[6%] h-full w-px hidden lg:block"></div>
  </div>

  <div class="content-container relative z-10">
    <SectionLabel text={content.eyebrow} />

    <h2
      id="method-title"
      class="mt-5 max-w-3xl font-display text-4xl font-light leading-[1.05] tracking-tight text-ink sm:text-5xl lg:text-6xl"
    >
      <MotionText as="span" class="block" type="reveal-line">
        {content.title}
      </MotionText>
    </h2>

    <div class="method-track relative mt-10 lg:mt-14">
      <!-- Linha de progresso -->
      <div class="method-line pointer-events-none absolute left-[11px] top-0 bottom-0 w-px bg-line sm:left-[15px] lg:left-1/2 lg:-translate-x-1/2" aria-hidden="true"></div>

      <div class="space-y-8 lg:space-y-12">
        {content.steps.map((step, i) => (
          <div
            class="method-step relative grid grid-cols-[auto_1fr] gap-5 sm:gap-7 lg:grid-cols-2 lg:gap-14"
            data-motion="reveal"
          >
            <!-- Marcador -->
            <div class="flex flex-col items-center lg:items-end">
              <div class="flex h-7 w-7 items-center justify-center border border-ink bg-ink text-white sm:h-9 sm:w-9">
                <span class="font-sans text-xs font-medium">{String(i + 1).padStart(2, "0")}</span>
              </div>
            </div>

            <!-- Conteúdo -->
            <div class="lg:col-start-2">
              <h3 class="font-display text-xl font-light text-ink sm:text-2xl lg:text-3xl">
                {step.title}
              </h3>
              <p class="mt-2 max-w-lg text-sm leading-relaxed text-graphite lg:text-base">
                {step.text}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
